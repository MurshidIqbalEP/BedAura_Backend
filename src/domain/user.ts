interface User{
    name:string,
    email:string,
    password:string,
    number:string,
    isBlocked:boolean,
    isVerified:boolean,
    isAdmin:boolean,
    isGoogle:boolean,
}

export default User;

